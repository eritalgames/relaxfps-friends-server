{
  "name": "relaxfps-friends-server",
  "version": "6.8.0",
  "description": "RELAXFPS backend with RFX economy, secure wallet, unlimited SSV rewards, paid Friends minutes, Google Play verification, Play Integrity, and Supabase persistence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check admin/app.js",
    "generate:totp": "node tools/generate-totp-secret.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
