{
  "name": "relaxfps-friends-server",
  "version": "4.2.0",
  "description": "RelaxFPS WebSocket server with friends, messages, calls, admin studio, announcements, promo codes and reward entitlements",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
