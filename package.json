{
  "name": "relaxfps-friends-server",
  "version": "6.13.0",
  "description": "RELAXFPS backend with stable preloaded ad-gated RFX tools, monthly feedback rewards, developer chat, secure wallet economy, announcements and device identity.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check admin/app.js",
    "generate:totp": "node tools/generate-totp-secret.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
