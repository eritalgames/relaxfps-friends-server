{
  "name": "relaxfps-friends-server",
  "version": "6.1.0",
  "description": "RELAXFPS WebSocket backend with secure RFX Token wallet, HMAC chained transaction ledger, Admin Studio, friends, groups, messaging and persistent usage tracking",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check admin/app.js",
    "generate:totp": "node tools/generate-totp-secret.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
