{
  "name": "relaxfps-friends-server",
  "version": "6.10.0",
  "description": "RELAXFPS backend with secure RFX economy, admin-managed promo codes, server-authoritative daily tasks, device identity recovery, overlay diagnostics, ad verification, Friends minutes, Google Play verification, Play Integrity, and Supabase persistence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check admin/app.js",
    "generate:totp": "node tools/generate-totp-secret.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
